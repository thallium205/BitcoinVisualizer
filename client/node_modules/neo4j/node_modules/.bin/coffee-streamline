#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../streamline/bin/coffee-streamline" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../streamline/bin/coffee-streamline" "$@"
  ret=$?
fi
exit $ret
