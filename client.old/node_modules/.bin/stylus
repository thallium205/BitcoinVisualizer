#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../stylus/bin/stylus" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../stylus/bin/stylus" "$@"
  ret=$?
fi
exit $ret
