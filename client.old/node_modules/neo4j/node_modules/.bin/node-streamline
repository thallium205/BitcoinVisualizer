#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../streamline/bin/node-streamline" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../streamline/bin/node-streamline" "$@"
  ret=$?
fi
exit $ret
