#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../streamline/bin/_node" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../streamline/bin/_node" "$@"
  ret=$?
fi
exit $ret
