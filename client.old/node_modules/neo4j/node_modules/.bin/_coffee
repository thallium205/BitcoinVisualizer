#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../streamline/bin/_coffee" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../streamline/bin/_coffee" "$@"
  ret=$?
fi
exit $ret
